{
  "name": "nfl-starter-guessing-game",
  "version": "1.0.0",
  "private": true,
  "description": "Guess the 5 starting skill players for one NFL divisional-round playoff winner, a new team each day.",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "test": "node --test",
    "dev": "vercel dev",
    "pull-data": "node scripts/pull-data.js"
  },
  "dependencies": {
    "csv-parse": "^7.0.1"
  },
  "devDependencies": {
    "vercel": "^54.21.1"
  }
}
